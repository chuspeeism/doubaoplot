# DoubaoPlot：在豆包工作里画科研图

DoubaoPlot 是在 [hang-jin/editaplot](https://github.com/hang-jin/editaplot) 基础上做的二次创作。
**整个项目的基底和灵感都来自作者 hang-jin** —— 让 AI 读懂科研数据、推荐图形、调用本机 Origin
画出可继续编辑的图，这套东西和它的全部实现都是他做的。我们在上面加的是「让你说清楚自己要什么图」这一层。

> **只能在 Windows 上用。** Origin 本身就是 Windows 软件，Mac、Linux、虚拟机都跑不了整条链路。

> **换别的 Agent 也能用。** 这是标准的 SKILL.md 技能，Codex、Claude Code 都能装，绘图能力一样。
> 只是安装目录解析、文档语气、提示词和选择器交互都是照着豆包用户做的，用别的 Agent 这些优化用不上。
> 下文写「豆包」的地方，换成你在用的 Agent 即可。

---

## 一、装

在豆包工作里发这句：

> 帮我安装 GitHub 上 chuspeeism 的 doubaoplot Skill

**Origin 得你自己先装好。** 这个 Skill 只调用你电脑上已经装好的 Origin/OriginPro，
它不替你安装、也不修改 Origin —— 你让它去装，它会拒绝。

**绘图引擎不在 Skill 里。** Skill 是「怎么画」的说明书，真正干活的引擎在完整仓库的
`runtime/` 目录下。宿主只把 Skill 子目录装进来时引擎不会跟过来，第一次画图会报
`engine_not_found`。遇到了它会自己把完整仓库取下来跑一次 setup，不用你动手，也不会为这一步问你。

要自己动手也行，就是这三条命令：

```powershell
git clone https://github.com/chuspeeism/doubaoplot.git
Set-Location doubaoplot
.\editaplot.cmd setup
```

`setup` 会找一个电脑上已有的 64 位 Python 3.10–3.12，建一个只属于本项目的环境，
再把 Skill 装到它探测到的宿主目录。不会用 git 就在 GitHub 页面下载 Source ZIP，
**完整解压**之后在那个目录里跑同一条 `setup`。

**装到哪了？** 安装脚本会按这个顺序找地方：

1. 你设了 `EDITAPLOT_SKILL_DIR` 环境变量 → 就装那儿
2. 本机已经存在的宿主 skills 目录（`~/.doubao/skills`、`~/.codex/skills` 这类）
3. 都找不到 → 回落到 `~/.codex/skills/doubaoplot`

装完它会把**实际选中的路径打出来**。如果豆包工作读的不是那个目录，设一下环境变量重装即可：

```
set EDITAPLOT_SKILL_DIR=<豆包工作的 skills 目录>\doubaoplot
```

（上游写死装到 `~/.codex/skills`，装进豆包会表面成功、实际白装。这是本版修的主要一处。）

---

## 二、两种用法

### 用法一：你有参考图 —— 照着复刻

看到一张顺眼的图，想用自己的数据画成那个样子。

把**参考图**和**你的数据文件**一起发给豆包，然后说：

> 帮我在 Origin 中，用我的数据复刻这张参考图的绘图风格

它会先分析参考图的版式、配色和绘图细节，再用你的数据画。

**参考图只影响样式，不制造数据。** 你数据里没有的东西，它不会替你编出来凑成参考图的样子。

### 用法二：你不知道该画什么图 —— 点着选

这是更常见的情况：数据有了，但不知道该画箱线图还是小提琴图，
更不知道该怎么跟 AI 描述才能一次到位。

直接调用这个 Skill，说：

> 我不知道该画什么图，帮我看看

它会在**豆包工作自己的内置浏览器里**打开**图表选择器** —— 45 张真机跑出来的 Origin 示例图铺开，
按方向分好类（材料表征、电化学、医学与机器学习、通用统计、分布形态……）。

页面只挂在本机地址上（`http://127.0.0.1:17864/`），不会另外弹一个 Chrome 窗口来打断你。

你点中一张，右侧立刻显示：

- **这张图需要你的数据里有哪几列** ← 最有用的一条
- 这条绘图路线的边界（哪些东西它不会替你算）
- 配色（会按你选的图自动推荐，色盲风险高的排在后面）
- 出图尺寸、误差标注、显著性星号这些细节

选完点「发送给豆包」就回去了；点「复制」也行，粘回对话框一样。

> 页面**永远先把内容放进你的剪贴板**，再去试自动发送。所以就算自动发送没成，
> 你也只是多粘贴一下，不会卡住。

---

## 三、它最后给你什么

**不是一张图片，是一个文件夹**，建在你数据文件旁边，叫 `<数据文件名>_EditaPlot_<时间戳>`。

里面你真正要的是这几个：

| 文件 | 是什么 |
|---|---|
| **`result.opju`** | **Origin 工程文件 —— 核心是它**。双击打开，颜色、坐标轴、图例、文字全都能接着改 |
| `result.png` | 插 Word、发群里 |
| `result.pdf` | 矢量，投稿常要 |
| `result.tif` | 期刊最常点名要的格式 |

**画完 Origin 窗口默认不关**，图就摆在那儿，可以直接上手改。

同一个文件夹里还有 `origin_verify_report.json` —— 画完之后回头去 Origin 里把坐标轴、
字体、图层重新读一遍的核对报告。不是画完就交差。

---

## 四、它不会替你做的事

这是上游作者定的边界，我们一条没改，也建议你别绕：

- 不替你平滑数据、删离群值、补峰
- 不替你算误差、拟合曲线、做显著性检验
- 不替你识别物相、算带隙、训练模型
- 缺哪一列就直接告诉你缺什么，**不会编一列出来凑数**

选择器里那些「不要替我算」的措辞，就是照着这条边界写的。

---

## 五、有 17 种图它画不了

Origin 本身画得出来，但这个 Skill 没写对应的绘图路线。完整清单在
[`skill/doubaoplot/references/not-covered.md`](skill/doubaoplot/references/not-covered.md)。

最容易被问到的是 **生存曲线（Kaplan-Meier）** —— 医学组里做了 ROC、PR、DCA、
校准曲线、混淆矩阵、Bland–Altman，唯独没有 KM。

还有 **PCA / t-SNE / UMAP**：能画，但降维坐标得你自己先算好，它只负责把算好的坐标
画成分组散点，不替你降维。

---

## 六、出问题了怎么办

| 现象 | 多半是 |
|---|---|
| 装完豆包工作里看不到这个 Skill | 装到 `~/.codex/skills` 去了 —— 设 `EDITAPLOT_SKILL_DIR` 重装 |
| 安装报「不是 EditaPlot Skill」或「写不进去」 | 旧版身份校验只认 `editaplot` 这个名字，装 DoubaoPlot 必失败。更新到最新版重装即可 |
| 选择器弹不出来 | 让它把打印出来的 `http://127.0.0.1:…` 地址在内置浏览器里打开；三个端口都被占时它会改给本地文件路径，自己双击打开，点「复制」再粘回来 |
| 点了「发送给豆包」没反应 | 内容已经在剪贴板里了，直接粘贴 |
| 卡在 Origin 启动 | 先跑 `editaplot.cmd doctor`，它会告诉你卡在哪一阶段 |
| 画出来不是想要的样子 | 在 Origin 里直接改 —— 这就是给你 OPJU 的意义 |

绘图本身的问题，先去[上游仓库](https://github.com/hang-jin/editaplot)看，那才是绘图能力的出处。

---

## 七、关于这个项目

DoubaoPlot 只是把 EditaPlot 搬进豆包工作，并在前面加了一层选图。**真正让它能画出顶刊质感科研图的，
是 hang-jin 写的 EditaPlot。** 觉得好用的话，去他的[仓库](https://github.com/hang-jin/editaplot)点个 star。

许可：Apache-2.0，与上游一致。完整改动声明见 [NOTICE](NOTICE)。
